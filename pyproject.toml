[project]
name = "notion-ory-agent"
version = "0.1.0"
description = "MCP Server with Notion and Ory Integration"
requires-python = ">=3.11"

[tool.black]
line-length = 88
target-version = ['py311']

[tool.isort]
profile = "black"